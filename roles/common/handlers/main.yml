---
# handlers file for common

- name: start firewalld
  service:
    name: firewalld
    state: started
    enabled: true

- name: reload firewalld
  service:
    name: firewalld
    state: reloaded
    enabled: true